# 线上配置文件
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:api}
    username: ${MYSQL_USERNAME:root}
    password: ${MYSQL_PASSWORD}
